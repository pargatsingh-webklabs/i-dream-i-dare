<p id="notice"><%= notice %></p>

<h1>Listing Admin Messages</h1>

<table>
  <thead>
    <tr>
      <th>From name</th>
      <th>From email</th>
      <th>Content</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @admin_messages.each do |admin_message| %>
      <tr>
        <td><%= admin_message.from_name %></td>
        <td><%= admin_message.from_email %></td>
        <td><%= admin_message.content %></td>
        <td><%= link_to 'Show', admin_message %></td>
        <td><%= link_to 'Edit', edit_admin_message_path(admin_message) %></td>
        <td><%= link_to 'Destroy', admin_message, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Admin message', new_admin_message_path %>
