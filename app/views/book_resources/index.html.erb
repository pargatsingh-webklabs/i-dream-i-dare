<p id="notice"><%= notice %></p>

<h1>Book Resources</h1>
<div style="color:red">"Active" resources will be displayed on the landing page, to be zipped into a file for user download. The files here are saved directly on the server (rather than on Amazon [AWS]), and so "Inactive" resources should be kept at a minimum if not needed.
</div>

<hr>
<p>
  <button>
    <%= link_to 'New Book resource', new_book_resource_path %>
  </button>
</p>

<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>Resource</th>
      <th>Active</th>
      <th>Path</th>
      <th>Filename</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @book_resources.each do |book_resource| %>
      <tr>
        <td><%= book_resource.id %></td>
        <td><%= book_resource.active %></td>
        <td><%= book_resource.path %></td>
        <td><%= book_resource.file_name %></td>
        <td><%= link_to 'Show', book_resource %></td>
        <td><%= link_to 'Edit', edit_book_resource_path(book_resource) %></td>
        <td><%= link_to 'Destroy', book_resource, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>