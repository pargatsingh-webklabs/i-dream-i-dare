<%= form_for(@bio_answer) do |f| %>
  <% if @bio_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bio_answer.errors.count, "error") %> prohibited this bio_answer from being saved:</h2>

      <ul>
      <% @bio_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="field">
      <b>Bio Question Id</b><br>
      <%= f.text_field :bio_question_id %>
    </div>

    <div class="field">
      <b>User ID</b><br>
      <%= f.text_field :user_id %>
    </div>
  </div>

  <div class="row" style="width:50%;">
      <%= f.label :answer_text %><br>
      <%= f.trix_editor :answer_text %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
